#include "binarysearch.h"

BinarySearch::BinarySearch(T array)
{
    this->array=array;
}

template<typename T>
BinarySearch::search(T data)
{
    search()
}

template<typename T>
BinarySearch::search(int l, int r, T data)
{

}
